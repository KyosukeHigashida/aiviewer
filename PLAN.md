# PLAN

## 次の実装イテレーション
スレッドペインを常に画面内へ表示し、PDF 側だけを独立してスクロールできる 2 ペイン構成まで進める。

## 今やる範囲
- デスクトップでは、PDF ペインとスレッドペインを独立してスクロールできるようにする。
- デスクトップでは、右ペインの新規スレッド作成部を常に見える位置に保つ。
- デスクトップでは、保存済みスレッド一覧を右ペイン内部だけでスクロールできるようにする。
- モバイルでは、画面下に常時見える 2 段階ボトムシートとしてスレッドペインを配置する。
- モバイルでは、閉状態でも新規スレッド作成部が見えるようにする。
- モバイルでは、展開時に保存済みスレッド一覧を確認できるようにする。
- 既存のローカル PDF 読込、再発見 UI、作成、保存、復元の流れを壊さずに適用する。

## 今回やらない範囲
- バックエンド API
- 認証・認可
- 複数人編集
- クラウド同期
- 複数 PDF 管理
- 注釈の共有、入出力
- 画像領域注釈
- AI 応答生成
- ドラッグ&ドロップ読込
- PDF 本体の永続化
- 起動時の自動復元
- ボトムシートのドラッグによる自由リサイズ
- スレッドの高度な検索、タグ、通知
- 右端ピンの精度向上
- 本文ハイライト精度の改善
- IndexedDB への移行
- README 作成

## 完了条件
- デスクトップ表示で、スレッドペインが常に画面内に表示される。
- デスクトップ表示で、PDF 側と右ペイン側を独立してスクロールできる。
- 長い PDF をスクロールしても、右ペインの作成部と一覧を見失わない。
- モバイル表示で、下固定のスレッドペインが常に見える。
- モバイル表示で、閉状態と展開状態を切り替えられる。
- 一覧クリックやピンクリックで PDF を移動しても、右ペインの表示位置を失わない。

## 履歴
### 完了: 最初の実装イテレーション
- React + TypeScript + Vite の最小構成を用意した。
- `react-pdf` で単一 PDF を表示できるようにした。
- 左に PDF、右にスレッドペインの 2 ペイン UI を作成した。
- PDF 上で選択したテキストを起点にスレッドを作成できるようにした。
- スレッドとコメントを `localStorage` に保存、復元できるようにした。
- 保存済みスレッドを右ペイン一覧で確認できるようにした。
- `react-pdf` の worker 読み込みを修正し、ローカル開発環境で PDF 表示を成立させた。

### 完了: 再発見 UI 改善
- スレッド作成時に `pageNumber`、`selectedText`、`contextBefore`、`contextAfter` を保存できるようにした。
- 保存済みスレッドに対応する位置マーカーを PDF 上の右端ピンとして表示できるようにした。
- 右ペインのスレッド一覧から、対象ページへスクロールし、対応するピンとスレッドを強調できるようにした。
- PDF 上の位置マーカーから、対応するスレッドを右ペインで見つけ直せるようにした。
- 本文テキストそのものの再ハイライトを追加した。
- 位置情報のない旧データを再発見対象外として区別表示できるようにした。

### 完了: ローカル PDF 読込対応
- ファイル選択 UI からローカル PDF を開けるようにした。
- PDF ごとの識別子として SHA-256 指紋を作成するようにした。
- 注釈スレッドを文書指紋ごとに保存、表示するようにした。
- 現在表示中の PDF に紐づくスレッドだけを右ペインと PDF 側に表示するようにした。
- PDF 未選択時の空状態を用意した。
- 旧データは区別表示し、現在の PDF と自動で混ざらないようにした。
