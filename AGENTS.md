# AGENTS

このプロジェクトで Codex は以下を守る。

- `SPEC.md` と `PLAN.md` を優先し、MVP 範囲を超える提案や実装をしない。
- 技術前提は React + TypeScript + Vite とする。
- 未確定の技術選定は、初回マイルストーンを最短で成立させる最小構成を優先する。
- 永続化はローカルファーストを前提とし、初期段階ではブラウザ内保存に限定する。
- バックエンド、認証、同期、共有は今回追加しない。
- ドキュメントは日本語で簡潔に書き、変更は小さく分ける。
